import { AntDesign } from '@expo/vector-icons';
import React from 'react';
import Colors from '../../constants/Colors';

export default function Check() {
  return <AntDesign name="check" size={24} color={Colors.themeColor} />;
}
