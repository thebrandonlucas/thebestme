import React from 'react';
import { StyleSheet } from 'react-native';
import { Text } from '../Themed';

export default function HomeTutorial() {
  return (
    <Text>
      Home is where we show you a snapshot of your day! You can see which habits
      you have completed, which habits you have remaining, and mood feedback and
      insights (along with some fancy charts and graphs of course).
    </Text>
  );
}

const styles = StyleSheet.create({});
